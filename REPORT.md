# Ananke v0.2 探索性点火报告

> 日期：2026-08-18  
> 性质：探索性仪器运行，非正式验证  
> 语料：LoCoMo `conv-26`，419 轮，19 sessions  
> 驱动模型：`openai-compatible|mistral-small-2603`  
> 运行回执：[`docs/IGNITION_RECEIPT_v0.2.md`](docs/IGNITION_RECEIPT_v0.2.md)

## 1. 两个观察

我最初的观察是：具有不同历史的系统，面对相似的压力，会走向不同的可实现未来。历史不只是造成了
“现在的样子”，它似乎还在改变“接下来哪些路走得通、哪些走不通”。我为此提出约束场理论作为解释框架。

这次仪器运行又产生了第二个观察：用于区分“外部选择”与“内部选择”的关键变量 EV，在两遍 419 轮运行中
一次也没有激活。这不是最初观察的同义重述，而是实验仪器对自身识别边界的一次显影：系统确实走向了
不同状态，但当前仪器没有产生足以说明这种分叉来自外部选择压力的证据。

## 2. 仪器与运行

Ananke 把记忆分为 WORKING、CONSOLIDATED 和 CORE 三层，以两种完整选择制度重放同一输入流。
Persistence 制度的第一道闸使用 `external_validation + internal_activation/e`；Frequency 制度使用
`total_activation`，不区分激活来源。两边共用语料、模型、prompt、嵌入、容量、关系分类逻辑与两级缓存，
以串行锁保证 P 先完成、F 后复用已付费的确定性结果。两遍日志均以 `run_metrics.completed=true` 结束，无 HTTP 错误或 429。

本次运行使用的关系分类器在 50 对开发集的点火前最终复跑中为 60.0% 一致率、κ=0.422，零解析失败。
但 duplicate 类 5 例全部漏判，contradict 类没有金标样本。因此该分类器可以让系统完整演化，却不能被视为已经通过
独立验收的测量仪器。

## 3. 观测结果

| 指标 | Persistence | Frequency |
|---|---:|---:|
| 轮次 / sessions | 419 / 19 | 419 / 19 |
| 第一闸升层 | 14 | 62 |
| 升 CORE | 6 | 17 |
| 终态 WORKING | 50 | 50 |
| 终态 CONSOLIDATED | 8 | 45 |
| 终态 CORE | 6 | 17 |
| `conflict_link` | 5 | 4 |
| 唯一被持续阻断记忆 | 1 | 2 |
| 终态 EV 总和 | 0 | 0 |
| HTTP 请求 | 1,005 | 153 |

归一化第一道闸集合为 `|P|=14`、`|F|=62`、`|P∩F|=14`、`P∖F=0`、`F∖P=48`、`|D|=48`，
Jaccard 为 14/62≈0.226。Frequency 升层数是 Persistence 的 4.4 倍，且 P 在本次运行中严格嵌套于 F。
这些是可审计的操作分叉，不是无差异结果。

但这个嵌套不应被上升为跨所有语料和动态路径的数学定理。对同一记忆的同一状态快照，Persistence 对 IA 乘
`1/e`，Frequency 的 `total_activation` 除了 IA 还包含 mergeable 和 contradict 等非 EV 关系事件，故计分层存在明确的
包含方向；然而两次运行的晋升、淘汰、召回和写入路径可以分叉，全局集合的嵌套仍是本次经验结果。

## 4. 主结果：制度分叉成立，选择压力来源不可识别

EV=0 使 Persistence 在本运行中只由 IA/e 推进，Frequency 则由更宽的非来源区分激活计数推进。
因此两套制度仍然不同，但区分它们的论题已从“外部选择 vs 内部选择”退化为“只接受 related 型 IA 且门槛更严”
与“接受更宽非 EV 激活且门槛更松”之间的差异。这次运行可以支持“仪器能完整演化，并且两制度产生明显不同的分层结构”；
它不能支持“External Selection 已被测试”，更不能支持 C2 或约束场理论本身。

## 5. EV=0 的三种解释与权重

EV=0 的三条候选解释现已部分分离（2026-08-18 五项实证检查，见 `DECISIONS_v0.2_freeze.md` §14）：

- **(a1) 冻结 prompt 对 duplicate 收窄是主因**：同一驱动模型（mistral-small-2603）在旧 prompt
  下判出过 4 条 duplicate，冻结 prompt 下 0 条——变量只有 prompt。日志中 4 次分类解析失败的
  原始输出均为 `"same fact"` / `"same frame"`，显示模型对近义重复的默认反应不在协议标签内，
  经 B7 重试改写后永远走不到 duplicate。
- **(a2) 模型交互为次因**：跨模型探针（qwen3.7-max vs mistral，4 对冻结 prompt）中，qwen 对
  同一句对判出 duplicate 而 mistral 输出 "same" 解析失败；但 mistral 在旧 prompt 下能判
  duplicate，故不是模型能力缺陷。
- **(b) 语料属性证据最弱且不可比**：v3 期的 63 次 EV 来自 qwen-plus + 余弦阈值判定（样本
  cosine 0.719 即触发），与 v4 的 duplicate 分类器机制不同，跨机制不可比，不能直接用于排除 (b)。

三者叠加方向一致：**duplicate 通道在当前操作化下结构性关闭，EV 不可显影**。现有数据仍不能
完全分离 (a) 与 (b)，但这个"无法区分"的边界已比二分时窄得多；它同样是本报告的主要讨论结果，
也是 v0.3 必须先修复 duplicate/EV 可观测性、再进入世界演化实验的理由。

## 6. 两项推测为什么都不可判

推测 1 要比较 `P∖F` 与 `F∖P` 的 judge evidence 命中率，但 `P∖F=0`。推测 2 要在 `F∖P` 中比较
EV=0 和 EV>0 子组，但该集合全部是 EV=0。这两个缺口都是集合结构缺失，不是 judge 分数尚未获取。因此分析器把两项推测都记为
`not_testable`，而不是成立或不成立。

若对 P 的 14 条和 F 的 62 条升层记忆全量跑 199 个 probes，需要 15,124 次 DeepSeek judge 请求，其中重叠集的
14 条会重复评估 2,786 次。这些请求只能填入 judge 分数，不能生成缺失的 `P∖F` 或 EV>0 子组，因而本次不执行全量 evaluate。
这是一个分析设计与成本同时受约束的停止决策，不是因结果不理想而取消评测。

## 7. 阻断、CORE 与计数口径

P/F 日志中有 181/384 条 `core_promotion_blocked`，但它们是每轮对已阻断记忆再次检查产生的审计记录，
不是 181/384 个独立矛盾。真实 `conflict_link` 为 P=5/F=4，被持续阻断的唯一记忆为 P=1/F=2。
两边最终仍有 6/17 条 CORE，因此“419 轮运行会使 CORE 趋零”没有在本次 `conv-26` 上发生。这不等于永久 conflict 阻断器的结构性风险已被消除。

## 8. 结论边界

本次运行完成了仪器建造目标：冻结的 prompt 与代码在长语料上完整运行，P/F 共享缓存按预期工作，三层记忆产生可审计演化，
并且两套制度形成明显不同的结构。它同时显示了测量设计的失效点：EV 没有显影，因而“外部选择与内部重复的差别”不可从本次数据中分离。

诚实的最强结论是：**在当前操作化下，Ananke 能完整演化并产生制度分叉，但 External Selection 与非 EV 激活无法分离；
这一不可识别性来自 duplicate 仪器盲区还是语料属性，现有数据不能回答。**

因此本报告不宣称 C2 获得支持，不宣称约束场理论获得验证，也不用分歧幅度代替对选择压力来源的识别。
本轮工作的主要成果是一件能运行、能拒绝不可判结论、并能把自身盲区记录为下一版问题的实验仪器。
